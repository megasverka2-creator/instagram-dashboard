name: Rich message sinovi (bir martalik)

on:
  workflow_dispatch:

jobs:
  sinov:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: sendRichMessage variantlarini sinash
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        run: python3 rich_sinov.py
